# Compiled stack

This repository is a scaffold. It does not own a CUDA or Rust runtime.

A Jacobi field is first-order variation, not finite distance. When that
experiment exists it may consume JSPT local maps / the later Rust gate.
Do not add a third finite-difference helper or a local GPU stack.
